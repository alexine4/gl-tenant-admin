import { Muted } from "@/components/ui/Muted";

export function Default() {
  return <Muted>Loading session…</Muted>;
}

export function ExtraSmall() {
  return <Muted size="xs">PNG, JPEG, WebP or GIF, up to 2 MB.</Muted>;
}
